{
  "name": "kit-command-center",
  "version": "1.0.0",
  "description": "KIT - Kim the Builder's JARVIS-style business operating system. Command center, specialized agents, KPI intelligence, remote MCP connector for Claude.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check server.js && node --check src/*.js && node --check src/modules/*.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.5.1",
    "ffmpeg-static": "^5.2.0",
    "helmet": "^8.1.0"
  }
}
